<!DOCTYPE html>
<html>
<head>
    <title>WitSeq</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- load stylesheet -->
    <link rel="stylesheet" type="text/css" href="/stylesheets/bootstrap3/bootstrap.min.css" />
    <link rel="stylesheet" type="text/css" href="/stylesheets/login/login.css" />
</head>
<body>
<div class="container-fluid">
    <form class="form-signin" role="form" onsubmit="return false;">
        <h2 class="form-signin-heading">Please sign up</h2>
        <input type="email" class="form-control" placeholder="Email" id="email" required autofocus>
        <input type="password" class="form-control" placeholder="Password" id="password">
        <input type="password" class="form-control" placeholder="rePassword" id="confirm_password">
        <div class="checkbox">
            <label>
                <input type="checkbox" value="remember-me"> Remember me
            </label>
        </div>
        <div class="signup"><a href="/login">已经有账号？</a></div>
        <button class="btn btn-lg btn-primary btn-block" type="submit" id="login_button">Sign in</button>
    </form>
</div>

<!-- jQuery文件。务必在bootstrap.min.js 之前引入 -->
<script src="http://cdn.bootcss.com/jquery/1.11.1/jquery.min.js"></script>
<!-- 最新的 Bootstrap 核心 JavaScript 文件 -->
<script src="http://cdn.bootcss.com/bootstrap/3.3.0/js/bootstrap.min.js"></script>
<script type="text/javascript">
    $(function () {
        $("#login_button").on('click',function (e) {
            var url = '/signup';
            var email = $('#email').val();
            var password = $("#password").val();
            var confirm_password = $("#confirm_password").val();
            var data = {email: email, password: password, confirm_password: confirm_password};
            $.post(url, data, function (data) {
                if(data.msg =="success") {
                    alert("注册成功！");
                    setTimeout(function () {
                        window.location.href = "/login";
                    },2000);
                }
            });
        })
    })
</script>
</body>
</html>